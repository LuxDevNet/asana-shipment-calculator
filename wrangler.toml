name = "asana-app"
main = "cloudflare-worker.js"
compatibility_date = "2024-03-01"

[vars]
# SUPABASE_URL = "https://your-project.supabase.co"
# SUPABASE_ANON_KEY = "your-supabase-anon-key"

# Use the service role key in production so the worker can read shipment_cogs_summary
# regardless of per-user RLS. Set via: wrangler secret put SUPABASE_SERVICE_ROLE_KEY
